import AppWrapper from './AppWrapper';
import HeroSection from './HeroSection';

const Home = () => {
  return (
    <AppWrapper>
      <HeroSection />
    </AppWrapper>
  );
};

export default Home;
